import { InstructorPipe } from './instructor.pipe';

describe('InstructorPipe', () => {
  it('create an instance', () => {
    const pipe = new InstructorPipe();
    expect(pipe).toBeTruthy();
  });
});
